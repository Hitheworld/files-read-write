{
  "name": "files-read-write",
  "version": "0.0.1",
  "description": "File to read and write",
  "main": "index.js",
  "scripts": {
    "watch": "mocha --watch tests/",
    "test": "mocha --reporter spec --timeout 2000 --recursive tests/",
    "test-cov": "istanbul cover ./node_modules/mocha/bin/_mocha -- -t 2000 --recursive  -R spec tests/",
    "test-html": "mocha --reporter mochawesome tests/"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Hitheworld/files-read-write.git"
  },
  "author": "ping",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Hitheworld/files-read-write/issues"
  },
  "homepage": "https://github.com/Hitheworld/files-read-write#readme",
  "dependencies": {
    "es6-promise": "^4.0.5"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "mocha": "^3.2.0",
    "mochawesome": "^2.0.4"
  }
}